meta {
  name: PUT faunas-detectadas {id}
  type: http
  seq: 1
}

put {
  url: {{baseUrl}}/api/faunas-detectadas/{{id}}
  body: json
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {"nombre": "Catalogo QA"}
}
