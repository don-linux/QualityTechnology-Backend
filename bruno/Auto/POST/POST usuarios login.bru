meta {
  name: POST usuarios login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/usuarios/login
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "nombre": "{{username}}",
    "contraseña": "{{password}}"
  }
}
