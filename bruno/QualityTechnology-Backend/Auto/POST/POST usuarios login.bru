meta {
  name: POST usuarios login
  type: http
  seq: 37
}

post {
  url: {{baseUrl}}/usuarios/login
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "nombre": "{{username}}",
    "contraseña": "{{password}}"
  }
}
